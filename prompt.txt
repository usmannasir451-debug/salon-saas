Supabase is changing their security policy. Starting October 30 2026, all tables need explicit GRANT statements to be accessible via the Data API. 

Please update all our SQL migration files and future table creation to include proper grants. Also generate a one-time SQL script that adds the required grants to ALL existing tables in our Snipforce project so we are compliant before the October 30 deadline.

The grants needed for each table are:
grant select, insert, update, delete on public.TABLE_NAME to authenticated;
grant select, insert, update, delete on public.TABLE_NAME to service_role;
grant select on public.TABLE_NAME to anon;

Generate this for all our existing tables: profiles, appointments, walk_ins, staff, services, clients, expenses, inventory, inventory_items, stock_adjustments, staff_salaries, payroll_entries, reviews, deals, deal_services, branches, salon_members, leads, notifications, audit_logs, loyalty_transactions

Also make sure all future migrations we create include these grants automatically.