Fix this console error: "Base UI: A component is changing the uncontrolled value state of Select to be controlled." This is happening in the Select components throughout the app. Find all Select components where the value prop could be undefined and replace undefined with empty string "" as the default value. Check appointments page, walk-in page, and calendar page especially. All Select value props must always be a string, never undefined.