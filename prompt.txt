You are an expert SaaS developer working on "Snipforce". Fix these two issues in Snipforce:

PART 1 - SUB-USER NAVIGATION FIX

Problem: Sub-user has access to services, staff, clients but clicking them redirects to appointments page.

Root cause investigation:
- Check lib/roles.ts - the custom permission system uses salon_members.permissions (JSONB array of page keys)
- The permissions array likely has keys like "appointments", "calendar", "walkin" but services/staff/clients keys may be different or missing
- Check what keys are being saved when owner creates sub-user permissions in team page
- Check PermissionGuard.tsx MODULE_PATH_MAP - verify services, staff, clients are mapped correctly
- Check Sidebar.tsx - verify services, staff, clients nav items are shown when permissions include them

Fix:
- Make sure permission keys exactly match the module keys used in PermissionGuard and Sidebar
- Services permission key should map to /services path
- Staff permission key should map to /staff path  
- Clients permission key should map to /clients path
- Test that sub-user with these permissions can navigate to all three pages without redirect

PART 2 - MEMBERSHIPS PAGE NOT SHOWING CONTENT

Problem: Memberships page exists but shows nothing - no plans, no packages, no UI visible.

Possible causes to check:
- SQL migration may not have been run yet - check if tables exist
- Module may not be enabled for the salon account
- Page may have a loading/error state that shows blank
- RoleContext may not be providing correct ownerId

Fix:
- Add proper empty states: "No membership plans yet. Create your first plan."
- Add loading spinner while data fetches
- Add error boundary to show what went wrong
- Make sure page works even if memberships tables do not exist yet (graceful error handling)
- Check if memberships module needs to be enabled in admin panel for the account
- Add a visible error message if Supabase query fails

Also: Run npm run build to confirm zero TypeScript errors after fixes.