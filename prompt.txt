Fix this runtime error on dashboard: "Zap is not defined". The Zap icon from lucide-react was removed from imports but is still being used somewhere in app/(dashboard)/dashboard/page.tsx. Either add Zap back to the lucide-react imports, or replace it with another appropriate icon that is already imported.