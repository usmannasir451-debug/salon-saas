Fix the admin panel at /admin with these issues:

1. FIX ACCOUNT CREATION ERROR
- "An unexpected response was received from the server" error when creating salon account
- Debug the createSalonAccount server action - check if SUPABASE_SERVICE_ROLE_KEY is being read correctly
- The admin client in lib/supabase/admin.ts needs to use the service role key to bypass RLS
- Add proper error logging to show exactly what error is occurring
- Fix the account creation so it works reliably

2. FIX SUSPEND/ACTIVATE NOT WORKING
- Suspend and Activate buttons are not working
- Fix the toggleSalonStatus server action
- Make sure it properly updates subscription_status in profiles table
- Show success/error toast after each action
- Refresh the salon list after status change

3. ADD CREDENTIALS MANAGEMENT
- After creating an account: show a modal with the credentials (email + password) with a "Copy Credentials" button that copies both to clipboard
- On each salon in the list: add a "Reset Password" button that lets admin set a new password for that salon
- Add a "View/Copy Login Link" button that copies the login URL + credentials as a formatted message ready to send via WhatsApp like:
  "Welcome to Snipforce! 
  Login URL: https://[site-url]/login
  Email: [email]
  Password: [password]
  Please change your password after first login."
- Store the initial password in profiles table as plain text (last_set_password field) so admin can always retrieve it
- Show this stored password in admin panel next to each salon with a "Show" eye button

4. ADD DIRECT PORTAL LINK
- Each salon row should have an "Open Portal" button that logs in as that salon (or at minimum opens the login page with email pre-filled)
- Add a copy email button next to each salon email

Generate SQL migration for last_set_password column in profiles table.