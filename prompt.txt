You are an expert SaaS developer working on "Snipforce". Fix and improve the custom permissions and sub-user system completely:

PART 1 - FIX SUB-USER SYSTEM ARCHITECTURE

Current problem: Sub-users created by salon owner are not properly linked to the salon and data is not shared correctly.

Fix this architecture:
- When owner creates a sub-user (cashier/manager etc), that sub-user logs in and sees THE SAME salon data as the owner
- Sub-user is NOT a separate salon - they work under the owner's salon
- All data (appointments, walk-ins, clients, staff, services) belongs to the owner's salon
- Sub-user just has limited access based on permissions given by owner
- In salon_members table: member_user_id links to auth.users, owner_id links to the salon owner's profile
- When sub-user logs in: system finds their salon_members record, gets owner_id, uses that owner_id to fetch all salon data

PART 2 - FIX NAVIGATION AND ROUTING FOR SUB-USERS

Current problem: Walk-in redirects to appointments for sub-users.

Fix:
- When sub-user logs in: check their permissions from salon_members table
- Build navigation sidebar showing ONLY their permitted pages
- All pages must use owner_id (not their own user_id) when fetching/saving data
- Fix walk-in page, appointments page, calendar page to work correctly for sub-users
- If sub-user tries to access a page they don't have permission for: show "Access Denied" and redirect to their first permitted page
- Create a useUserContext hook that returns: isOwner, ownerId, permissions, role_name for use across all pages

PART 3 - OWNER-ONLY FEATURES

These features should NEVER be accessible to sub-users regardless of permissions given:
- Team Members page (/team) - only owner can manage users
- Settings page (/settings) - only owner can change salon settings
- Payroll page (/payroll) - only owner can see salaries
- Admin panel (/admin) - only super admin

Even if owner accidentally gives these permissions - system should block sub-users from these pages.

PART 4 - IMPROVE TEAM MEMBER CREATION FLOW

When owner creates a new team member:
- Form fields: Display Name (e.g. "Front Desk Manager"), Email, Password
- Permission checklist (checkboxes):
  □ Dashboard - View salon overview and KPIs
  □ Appointments - View and manage bookings
  □ Calendar - View appointment calendar
  □ Walk-In - Process walk-in customers and payments
  □ Services - View and manage services and deals
  □ Staff - View and manage staff members
  □ Performance - View staff performance reports
  □ Reviews - View customer reviews
  □ Clients - View and manage client profiles
  □ Inventory - View and manage inventory
  □ Expenses - View and add expenses
  □ Reports/P&L - View financial reports
- Quick preset buttons:
  * "Cashier Setup" - auto-ticks: Appointments, Calendar, Walk-In
  * "Manager Setup" - auto-ticks: Dashboard, Appointments, Calendar, Walk-In, Services, Staff, Clients
  * "Receptionist Setup" - auto-ticks: Appointments, Calendar, Clients
- After creation: show credentials summary with copy button and WhatsApp share button
- Owner can edit permissions anytime
- Owner can reset password of any sub-user
- Owner can deactivate/delete sub-user

PART 5 - ADMIN PANEL - SALON DETAIL WITH SUB-USERS

In admin panel when viewing a salon:
- Show "Team Members" section listing all sub-users of that salon
- Each sub-user row shows: display name, email, permissions list, last set password (with eye toggle), status (active/inactive)
- Admin can see exactly who has access to each salon
- Show total sub-user count on the main salons list

PART 6 - DATA ISOLATION

Make sure:
- Sub-user saving a new appointment: saved under owner's salon (using owner_id)
- Sub-user processing walk-in: saved under owner's salon
- Sub-user viewing clients: sees owner's salon clients only
- Sub-user viewing staff: sees owner's salon staff only
- No data leakage between different salons

TECHNICAL REQUIREMENTS:
- Fix RLS policies to allow sub-users to read/write owner's salon data
- Update all pages to use effective owner_id when querying
- salon_members table must have: owner_id, member_user_id, display_name, permissions (JSONB array), status, last_set_password
- Generate complete SQL migration at the end
- Mobile responsive
- Maintain pink/rose theme