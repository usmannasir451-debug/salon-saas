You are an expert SaaS developer working on "Snipforce". The dashboard is completely broken with filter changes. Do a full review and fix everything. This is critical.

PART 1 - CRITICAL: FIX ALL DASHBOARD DATA FETCHING

ROOT CAUSE INVESTIGATION:
- Check how date ranges are being passed to Supabase queries
- Today filter: should use TODAY's date only (appointment_date = today, DATE(created_at) = today)
- This Week: Monday to Sunday of current week
- This Month: first day to last day of current month
- Previous Month: first day to last day of previous month
- Custom Range: user selected start and end dates
- PROBLEM: likely the queries are not re-running when filter changes, or date format is wrong

FIX ALL QUERIES:
- Every single query on dashboard must re-run when dateRange changes
- Use useEffect with dateRange as dependency for ALL data fetching
- Appointments query: filter by appointment_date >= range.from AND appointment_date <= range.to
- Walk-ins query: filter by created_at >= range.from + 'T00:00:00' AND created_at <= range.to + 'T23:59:59'
- Membership transactions: filter by created_at >= range.from + 'T00:00:00' AND created_at <= range.to + 'T23:59:59'
- Expenses query: filter by date >= range.from AND date <= range.to
- Make sure date formats match exactly what Supabase expects (YYYY-MM-DD for dates, ISO string for timestamps)

VERIFY EACH TILE INDEPENDENTLY:
- Total Sales tile: test with Today, This Week, This Month, Previous Month, Custom Range
- Appointments count tile: same
- Walk-ins count tile: same
- Discounts tile: same
- Memberships tile: same (if enabled)
- Each tile must show correct non-zero data when data exists for that period

PART 2 - FIX SERVICE POPULARITY AND STAFF UTILIZATION

SERVICE POPULARITY:
- Query appointment_services and walk_in_services tables for the selected date range
- Join with services table to get service names
- Count bookings per service and sum revenue per service
- Show top 5 services by booking count as horizontal bar chart
- If no data: show "No services data for this period" message not blank

STAFF UTILIZATION:
- Query appointments and walk_ins for selected date range
- Count total bookings per staff member
- Calculate utilization: (booked slots / total available slots) as percentage
- Show as progress bars per staff member
- If no data: show "No staff data for this period" message

PART 3 - COMPLETE DASHBOARD REDESIGN

Remove these tiles/sections completely:
- Remove "Pending Appointments" tile
- Remove any redundant or confusing tiles
- Remove anything showing raw IDs or codes

ADD THESE ANALYTICS TILES (all respect date range filter):

ROW 1 - KEY METRICS (4 tiles):
1. Total Revenue - appointments + walk-ins + memberships, with % change vs previous period
2. Total Appointments - count with % change vs previous period
3. Total Walk-Ins - count with % change vs previous period
4. Discounts Given - total discount amount for period

ROW 2 - FINANCIAL (3 tiles):
5. Net Revenue - Total Revenue minus Discounts minus Loyalty Redeemed
6. Total Expenses - sum of expenses for period (only if expenses module enabled)
7. Net Profit - Net Revenue minus Expenses (only if expenses module enabled)

ROW 3 - CLIENTS (2 tiles):
8. New Clients - clients who visited for first time in this period
9. Returning Clients - clients who visited before this period

ROW 4 - CHARTS (2 columns):
10. Revenue Trend - line chart, last 12 months monthly revenue
11. Sales by Source - donut chart: Appointments vs Walk-ins vs Memberships (revenue breakdown)

ROW 5 - CHARTS (2 columns):
12. Service Popularity - horizontal bar chart, top 5 services by bookings
13. Payment Methods - bar or donut chart showing cash vs card vs other

ROW 6 - STAFF AND OPERATIONS:
14. Staff Performance - table showing each staff: appointments count, walk-ins count, revenue, avg rating
15. Busy Hours Heatmap - 8AM to 10PM by day of week

ROW 7 - ALERTS AND QUICK INFO:
16. Low Stock Alerts - inventory items below reorder level (if inventory module enabled)
17. Upcoming Appointments Today - list of today's appointments regardless of date filter
18. Expiring Memberships - clients whose memberships expire within 7 days (if memberships module enabled)

WELCOME HEADER:
- "Welcome, [Salon Name]! 👋"
- Current date and live time
- Branch filter (All Branches + individual branches)
- Date range filter: Today, This Week, This Month, Previous Month, Custom Range

MODULE-BASED VISIBILITY:
- Expenses tiles: only if expenses module enabled
- Memberships tiles: only if memberships module enabled
- Inventory alerts: only if inventory module enabled
- Reports charts: only if reports module enabled

PART 4 - PERCENTAGE CHANGE CALCULATIONS

For each metric tile, show comparison vs previous period:
- Today selected: compare to yesterday
- This Week: compare to last week
- This Month: compare to last month
- Previous Month: compare to month before that
- Custom Range: compare to same duration before selected range
- Show: green arrow up with +X% if improved, red arrow down with -X% if decreased
- Show "N/A" if no previous period data

PART 5 - EMPTY STATES

Every chart and tile must have proper empty state:
- When no data for selected period: show friendly message with icon
- Never show blank/white space
- Never show NaN, undefined, or 0 without explanation
- Example: "No appointments this week. Start booking to see data here."

PART 6 - PERFORMANCE

- All dashboard queries run in PARALLEL using Promise.all - not sequential
- Show skeleton loading state while data fetches
- After initial load: only re-fetch changed data when filter changes
- Add error handling: if query fails, show error message not blank

TECHNICAL REQUIREMENTS:
- No new SQL migrations needed
- Run npm run build to confirm zero TypeScript errors
- Update CLAUDE.md
- Mobile responsive - cards stack vertically on mobile
- Dark mode compatible
- All amounts use useCurrency hook
- No hardcoded currency symbols