You are an expert SaaS developer working on "Snipforce". Fix these three issues:

PART 1 - FIX PAYROLL PRORATA ERROR

Error: "Could not find the 'prorata_detail' column of 'payroll_entries' in the schema cache"

Fix:
- Check payroll_entries table structure in the codebase
- The code is trying to insert/select prorata_detail column but it does not exist in the database
- Either add the missing column to the migration OR remove it from the code - choose whichever is the correct fix based on the architecture
- Make sure payroll generation works without any schema errors
- Test that monthly payroll can be generated successfully for all staff members

PART 2 - FIX DASHBOARD REVENUE CALCULATION BUG

Problem: "All Branches" total revenue shows 300,000+ but individual branches combined show less than 100,000 - numbers are inconsistent.

Root cause investigation and fix:
- Check how "All Branches" revenue is calculated vs individual branch revenue
- Walk-ins and appointments may be getting double counted in "All Branches" view
- Some records may have null branch_id causing them to appear in "All Branches" but not in any specific branch - these should still be counted correctly
- Fix the revenue aggregation logic:
  * "All Branches" = sum of ALL appointments + walk-ins for this owner (regardless of branch)
  * Single branch = sum of appointments + walk-ins WHERE branch_id = selected branch
  * Records with null branch_id should appear in "All Branches" only, not in specific branch views
- Apply same fix to: revenue KPIs, expense calculations, net profit, all charts
- Make sure no double counting anywhere
- Test with multiple branches to confirm numbers add up correctly

PART 3 - ADD CUSTOM FAVICON

- Create a custom favicon for Snipforce using a scissors icon in pink/rose color (#e11d48 or similar)
- Generate favicon in multiple sizes: 16x16, 32x32, 180x180 (apple touch icon)
- Add favicon.ico, apple-touch-icon.png to the /public folder
- Update app/layout.tsx metadata to include all favicon references
- Replace the current default Next.js/Vercel favicon completely
- The scissors icon should be simple, clean and recognizable at small sizes
- Use SVG-based approach for crisp rendering at all sizes

TECHNICAL REQUIREMENTS:
- Generate SQL migration for prorata_detail column if needed
- Fix must not break existing payroll data
- Dashboard fix must work for single branch salons too
- Mobile responsive
- No breaking changes to existing functionality