The Team Members invite form shows "Requires SUPABASE_SERVICE_ROLE_KEY in .env.local for email delivery" even though the key is set in .env.local. Fix this by checking how the team invite component reads this environment variable. The issue is likely that SUPABASE_SERVICE_ROLE_KEY is a server-side variable but the component is checking it client-side. Fix the check so it properly detects the key on the server side and removes this warning when the key exists.