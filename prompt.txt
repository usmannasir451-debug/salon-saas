You are an expert SaaS developer working on "SalonPro". Fix all the following issues found during testing:

PART 1 - DASHBOARD WALK-IN TILE FIX
- The "New Walk-in" / "Quick POS" tile on dashboard is overlapping with the Today/This Week stats tiles - fix alignment so it sits properly and does not overlap
- Make the walk-in tile smaller and minimalist - just show "Quick POS" or "New Walk-in" with a clean icon, remove the cash/card/jazzcash/easypaisa text from the tile itself
- The tile should look clean and separate from the stats row

PART 2 - NOTIFICATION PANEL POSITION FIX
- Notification dropdown is appearing off-screen or in wrong position on laptop
- Fix the dropdown positioning so it always appears fully visible within the screen
- Use a right-aligned dropdown that opens downward and stays within viewport bounds

PART 3 - STAFF MEMBER SHOWING CODE INSTEAD OF NAME
- In appointments and walk-in forms, the Staff Member field is showing a UUID/code instead of the staff name after selection
- Fix all staff dropdowns throughout the app to always display staff name, never the ID or UUID

PART 4 - SYNC WALK-IN, APPOINTMENT, CALENDAR AND CLIENTS
- Walk-in discount field: currently only showing percentage option - add both options: fixed PKR amount AND percentage, same as appointments
- Appointment "Mark as Paid" modal: add payment method selection (Cash, Card, EasyPaisa, JazzCash, and any custom payment methods from settings) - currently missing payment method
- Walk-in "Mark as Paid": also ensure payment method is selectable
- Calendar view: ensure it is fully synced with appointments data
- Client lookup by phone number: when phone number is entered in walk-in or appointment form, auto-load client details properly - fix any integration issues

PART 5 - CUSTOM PAYMENT METHODS SYSTEM
- In /settings page: add a "Payment Methods" section where owner can manage payment methods
- Pre-loaded defaults: Cash, Card, EasyPaisa, JazzCash
- Owner can add custom payment methods by typing a name (e.g. "HBL Machine", "Meezan Bank", "SadaPay")
- Owner can enable/disable each payment method
- All payment method dropdowns throughout the app (walk-in, appointments mark paid) must pull from this settings list
- Save payment methods in profiles table as a JSON array

PART 6 - PAYMENT METHOD ANALYTICS ON DASHBOARD
- Add a new KPI card on dashboard: "Revenue by Payment Method"
- Show breakdown: Cash: PKR X, Card: PKR X, EasyPaisa: PKR X, JazzCash: PKR X, etc.
- This should reflect actual payment_method recorded on walk_ins and appointments
- Filter by Today / This Week / This Month same as other KPIs
- Also add this breakdown to the P&L report

PART 7 - DEALS NOT SHOWING IN APPOINTMENTS AND WALK-INS
- Deals created in Services > Deals tab are not appearing in appointment booking form or walk-in form
- Fix appointment booking form: add a "Select Deal" option alongside individual services
- Fix walk-in form: add "Select Deal" option
- When a deal is selected: show all included services, show deal price, calculate total correctly
- Deal and individual services should be mutually exclusive per line item but user can mix (e.g. one deal + one extra service)
- Make sure deals are properly fetched from the deals table with their associated services

TECHNICAL REQUIREMENTS:
- Ensure payment_method column exists on both walk_ins and appointments tables
- Add payment_methods column to profiles table if not exists (JSON array)
- Generate SQL for any missing columns
- All fixes must be mobile responsive
- Maintain pink/rose theme
- Do not break any existing functionality