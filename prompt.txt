You are an expert SaaS developer working on "Snipforce". Make these fixes and improvements carefully. Review entire codebase before implementing. Ensure no data is affected. Run npm run build to confirm zero errors before finishing.

PART 1 - DASHBOARD FIXES

ADD PREVIOUS MONTH TILE:
- Add "Previous Month" as a filter option alongside Today/This Week/This Month/Custom Range
- When selected: show all KPIs for the previous calendar month
- Label should show month name: "April 2026"

FIX CUSTOM RANGE AND PREVIOUS MONTH NUMBERS:
- CRITICAL: Revenue calculation must include ALL three sources for ANY date range:
  * appointments WHERE appointment_date BETWEEN range.from AND range.to AND total_amount IS NOT NULL
  * walk_ins WHERE DATE(created_at) BETWEEN range.from AND range.to
  * membership_transactions WHERE type='payment' AND DATE(created_at) BETWEEN range.from AND range.to
- Current bug: custom range only shows membership data - fix appointments and walk-ins query to use correct date field
- Appointment count: use appointment_date field
- Walk-in count: use DATE(created_at)
- All counts and revenue must use same date range consistently
- Test: previous month numbers must match sum of individual days in that month

ADD SALES TREND CHART:
- Monthly sales trend line chart showing last 12 months
- X axis: months (Jan, Feb, Mar...)
- Y axis: total revenue
- Filter: select year (dropdown showing available years)
- Line chart using recharts
- Show appointments + walk-ins + membership revenue combined per month
- Place below existing charts on dashboard

ADD DISCOUNT TILE:
- New KPI tile: "Discounts Given" showing total discount amount for selected period
- Source: appointments.discount_amount + walk_ins.discount_amount
- Show in same currency format
- Respect Today/This Week/This Month/Previous Month/Custom Range filter

PART 2 - MANDATORY FIELDS FIX

WALK-IN POS - MAKE STAFF MANDATORY:
- Staff selection must be required before completing order
- Show validation message if staff not selected: "Please select a staff member"
- Block "Complete & Save" button if staff not selected

APPOINTMENTS - MAKE ALL FIELDS MANDATORY:
- Client name: required
- Client phone: required
- Staff: required
- Branch: required (when multiple branches exist, auto-assign for single branch)
- Show clear validation messages for each missing field
- Block save if any required field missing

FIX ID/CODE SHOWING IN DROPDOWNS:
- After selecting Staff in walk-in: show staff NAME not UUID
- After selecting Branch in walk-in: show branch NAME not UUID
- After selecting Staff in appointments: show staff NAME not UUID
- After selecting Branch in appointments: show branch NAME not UUID
- After selecting Branch in expenses: show branch NAME not UUID
- Root cause: Select components value prop showing id but SelectValue showing code
- Fix all Select components to display human-readable label after selection
- Check ALL Select/dropdown components in: walkin/page.tsx, appointments/page.tsx, expenses/page.tsx, calendar/page.tsx

EXPENSES - BRANCH MANDATORY:
- Branch field required when salon has multiple branches
- Auto-assign single branch for single-branch salons
- Show validation if branch not selected
- Fix branch dropdown to show branch NAME not ID after selection
- Remove "Paid By" field completely
- Remove "Approval Status" field completely

PART 3 - EXPENSE MODULE IMPROVEMENTS

FIX RECEIPT UPLOAD:
- Create "expense-receipts" storage bucket in Supabase if it doesn't exist
- Add this to the SQL migration: create the bucket via SQL or add instructions
- Actually: add code to create bucket programmatically if it doesn't exist using admin client
- After successful upload: store receipt_url in expenses table
- View/Download receipt: add eye icon button on expense row that opens receipt in new tab
- Download button: download the receipt file

FIX RECURRING EXPENSES:
- Check if recurring expense functionality actually works
- Recurring expense: when marked as recurring, it should auto-create the same expense next month
- Implement a check on dashboard load: if any recurring expenses are due today or past due, auto-create them
- Show notification when recurring expenses are auto-created
- Add "Last Created" date to recurring expense display

PART 4 - APPOINTMENT MODULE SIMPLIFICATION

REMOVE BILLING FROM APPOINTMENTS:
- Remove "Mark as Paid" button from appointments completely
- Remove payment method selection from appointments
- Remove discount application from appointment modal
- Appointment statuses: Pending, Confirmed, No Show only
- Remove "Done/Completed" status from appointments (payment happens via walk-in)
- Keep: book appointment, confirm, cancel, no-show
- Update appointment_date filtering and calendar accordingly

CONNECT APPOINTMENTS TO WALK-IN:
- On appointments list: add "Check In / Bill" button for Confirmed appointments
- Clicking "Check In" opens walk-in POS with client details pre-filled:
  * Client name auto-filled
  * Client phone auto-filled
  * Services from appointment pre-selected in cart
  * Staff from appointment pre-selected
  * Branch from appointment pre-selected
- User can add/remove services in walk-in, apply discounts, process payment
- After walk-in completed: appointment status automatically updated to "Completed"
- This creates a proper appointment → walk-in billing flow

ALIGN NO-SHOW STATUS:
- Appointment statuses must be: pending, confirmed, no_show, cancelled, completed
- completed = set automatically when linked walk-in is processed
- Calendar filter for no_show must work correctly
- Appointment form must have these exact status options

PART 5 - CALENDAR VIEW IMPROVEMENTS

STAFF-WISE CALENDAR VIEW (like Mangomint):
- Add a new view mode: "Staff View" toggle button alongside Day/Week
- Staff View (Day): show one column per staff member side by side
  * Each staff has their own column with their name as header
  * Appointments shown as cards in respective staff column
  * Time slots on left Y axis (8AM to 10PM)
  * Drag and drop to reassign staff (optional, nice to have)
- Staff View (Week): show staff selector dropdown, view one staff week at a time
- Existing Day/Week views: show staff name ON the appointment card (currently missing)
- Appointment card shows: client name, service name, staff name, time

PART 6 - TEAM MEMBER BRANCH ASSIGNMENT

When creating sub-user:
- Add "Assigned Branch" field (dropdown of branches)
- Optional: if not selected, sub-user sees all branches
- If selected: sub-user only sees data for their assigned branch
  * Walk-in: branch auto-selected and locked to their branch
  * Appointments: branch auto-selected and locked
  * Dashboard: shows only their branch data
  * Reports: filtered to their branch
- Store branch_id in salon_members table
- Add branch_id column to salon_members if not exists

FIX SUB-USER PERMISSION NAVIGATION:
- CRITICAL BUG: clicking Staff, Services, Clients redirects to Appointments
- Root cause: PermissionGuard firstAllowed logic or ROLE_NAV missing these paths
- Fix: when sub-user has permission for staff/services/clients, those pages must be accessible
- Debug PermissionGuard - check if module keys match permission keys exactly
- Check team/page.tsx ALL_PERMISSIONS list - ensure staff, services, clients keys are correct
- Check Sidebar MODULE_HREFS - ensure staff → /staff, services → /services, clients → /clients
- Test each permission individually after fix

PART 7 - PNL IMPROVEMENTS

- Add separate line items in P&L:
  * Gross Revenue (appointments + walk-ins + memberships)
  * Less: Discounts Given (total discount_amount)
  * Less: Loyalty Redeemed (total loyalty points redeemed as currency)
  * Net Revenue
- These should also show in revenue section of reports

PART 8 - WALK-IN ORDERS VIEW

- Walk-in POS has Orders tab showing today's orders
- Add date navigation: < Previous Day | [Date] | Next Day >
- Show orders for selected date
- Default: today
- Each order shows: time, client name/phone, services, staff, total, payment method
- Cannot navigate to future dates

TECHNICAL REQUIREMENTS:
- Generate SQL migration for:
  * branch_id column on salon_members table
  * Ensure appointment statuses are aligned (no_show, completed, pending, confirmed, cancelled)
- Run npm run build to confirm zero TypeScript errors
- Update CLAUDE.md with all changes
- Mobile responsive throughout
- Dark mode compatible
- No breaking changes to existing data
- All existing appointments/walk-ins data preserved